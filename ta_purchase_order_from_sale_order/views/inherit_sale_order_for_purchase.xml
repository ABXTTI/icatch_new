<?xml version="1.0"?>
<odoo>
<data>
<!-- FORM VIEW  -->
        <record id="create_purchase_order_sale_order_view" model="ir.ui.view">
            <field name="name">sale.order.form</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                <header>
                    <button name="create_purchase_order" string="Create Purchase Order" type="object" class="btn-primary" attrs="{'invisible':[('state','in',['done','cancel'])]}"/>
                    <button name="create_tax_purchase_order" string="Visibility Tax PO" type="object" class="btn-primary" attrs="{'invisible':[('state','in',['done','cancel'])]}"/>
                </header>
            </field>
        </record>
</data>
</odoo>

